# TailorApp Print Bridge

Local desktop agent that lets the TailorApp web app print tags,
receipts, and thermal labels on printers attached to an outlet PC.

See `docs/` for the spec. See `CLAUDE.md` for repo conventions.